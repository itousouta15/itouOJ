# 斷網比賽操作手冊

機房有區域網路（電腦之間互通）但沒有對外 internet 時的完整流程。

判題本來就跑在本機（sandbox-runner `127.0.0.1:8090`、Piston `localhost:2000`），
資料庫是本地 SQLite，所以斷網不影響判題核心。真正需要處理的是登入方式、
外部字型，以及機房的網路設定。

---

## 一、賽前一週

### 1. 確認每個選手帳號都能用密碼登入

**這是最容易在比賽當天爆掉的一項。** Google / Discord 登入需要對外網路，
斷網時完全不能用；只用 OAuth 註冊的帳號沒有 `passwordHash`，當天會直接登不進去。

```bash
node scripts/contest-accounts.mjs list
```

輸出會標出每個帳號「斷網可登入」與否。看到 `✗ 只能 OAuth` 就要處理：

```bash
# 幫所有沒密碼的帳號補上隨機密碼
node scripts/contest-accounts.mjs set-password --all-oauth-only
```

補密碼**不會**影響他們原本的 Google / Discord 登入，賽後照樣能用。

### 2. 批次建立選手帳號

```bash
# 建立 oj01 ~ oj30
node scripts/contest-accounts.mjs create --prefix oj --count 30

# 或指定名單
node scripts/contest-accounts.mjs create --names alice,bob,carol
```

密碼會隨機產生（去掉 `i l o 0 1` 這類容易看錯的字元），同時輸出到畫面和
`contest-accounts-<時間>.csv`，可直接列印裁成紙條發給選手。

> 資料庫只存 bcrypt hash，**關掉視窗後就再也查不回明碼**。CSV 請保管好，
> 賽後刪除。

### 3. 建立比賽並讓選手報名

在 `/admin/contests` 建比賽、加題目、設定起訖時間與凍結分鐘數。

兩個跟斷網比賽直接相關的設定：

- **計分方式選 IOI**。賽後才整批判題，選手當下看不到結果，ICPC 的錯誤罰時會變成
  盲罰。IOI 是每題只看最後一次提交、不罰時。
- **可用語言**照比賽規則勾選（例如只勾 C++）。勾了之後網頁提交、測試執行、
  離線收件程式三條路都只收這些語言；全部不勾 = 不限制。

**選手必須事先加入比賽**（`/contests` 頁面用加入代碼），否則提交會被擋 403。
建議賽前就用管理員帳號確認每個人都在參賽名單裡。

### 4. 確認判題環境不需要連外

- C / C++ / Python / JavaScript 走自家的 sandbox-runner，安全。
- **Java 15 仍走 Piston**，確認 runtime 已經預先安裝好；Piston 首次執行某語言
  時可能會想上網抓套件，斷網就會失敗。
- 賽前拔掉網路線，每種語言各交一題測試。

---

## 二、賽前一天：設定斷網模式

在 `.env` 加入：

```
OFFLINE_MODE=1
```

開啟後：

- 登入頁 / 註冊頁 / 設定頁的 Google、Discord 按鈕**整組隱藏**
- 直接打 `/api/auth/google` 會導回本地登入頁，不會嘗試連外
- 網頁不再載入 Google Fonts 與辰宇落雁體（`font.emtech.cc`）

字型的部分要說明一下：所有 `font-family` 都留了 `system-ui` / `serif` /
`monospace` 後備，所以斷網時畫面會改用 Windows 內建字型，**看起來比平常樸素，
但版面不會壞、而且載入快很多**。若不開這個開關，每頁都要等外部字型 timeout
（最久 4 秒）才顯示。

> 建議寫進 `.env` 而不是只在啟動時帶環境變數，這樣 `next build` 和
> `next start` 都看得到，未來就算某個頁面變成靜態渲染也不會出錯。

---

## 三、比賽當天：啟動

### 1. 判題機網路設定

- 設**固定 IP**（例如 `192.168.1.50`），不要用 DHCP，免得重開機後位址變動
- Windows 防火牆放行 TCP `3000`
- 記下 IP，寫在白板上給選手

### 2. 啟動伺服器

```bash
npm run build
npx next start -H 0.0.0.0 -p 3000
```

`-H 0.0.0.0` 是關鍵，否則只有判題機自己連得到。**不要用 `npm run dev`**，
正式模式快很多。

### 2b. 開賽前的統一檢查（重要）

每台選手機的收件程式設定完成後，會自動向伺服器回報「已就緒」。
開賽前打開 **`/admin/contests/<比賽id>/status`**，確認：

```
已報名  30      收件程式已就緒  30 / 30      比賽狀態  尚未開始
```

只要不是 `30 / 30` 就代表有機器沒設定好。頁面會列出每個人的回報時間和電腦名稱，
可以直接對照是機房哪一台。**確認全部就緒再宣布開始**——漏掉一台，
那位選手整場都無法提交，而且要到賽後上傳時才會發現。

### 3. 連線測試

從任一台選手機用瀏覽器開 `http://192.168.1.50:3000`，確認：

- [ ] 網站正常顯示，沒有卡在載入畫面
- [ ] 登入頁**看不到** Google / Discord 按鈕
- [ ] 用紙條上的帳密可以登入
- [ ] 看得到比賽和題目
- [ ] 交一題假的答案，確認判題有結果、計分板有更新

### 4. 拔網路線實測

對外網路實際切斷後，把上面整套再跑一次。在有網路的環境測不算數。

---

## 四、已知風險

### 判題佇列是單執行緒的（最需要注意）

`src/lib/judge.ts` 的 `enqueueSubmission` 用一條 promise chain 串起所有提交，
**全站同一時間只有一筆在判**。這是刻意的設計（避免壓垮 4 核機器），但如果
30 個選手在最後 10 分鐘同時交，每筆要跑 10 筆測資，最後一個人可能要等好幾分鐘。

緩解方式：

- **短期**：控制每題測資數量、壓低時限，讓單筆判題盡量在 2~3 秒內完成
- **中期**：改成 N 條平行 chain（N = CPU 核心數 − 1）。要注意平行執行會互相
  干擾 CPU time 量測，改完必須重新校正時限

### 頁尾的外部連結

Footer 有 itousouta.me、GitHub、X、Telegram 等外部連結。斷網時點了會開不起來，
但它們只是 `<a href>`，**不會拖慢或阻擋網頁渲染**，可以不管。

### 時間

所有時間戳（比賽起訖、罰時）都來自判題機這一台，所以選手機時鐘不準沒關係。
但判題機自己的時鐘要對，斷網後沒有 NTP 校時，賽前先確認一次。

---

## 五、疑難排解

**選手說登不進去**
```bash
node scripts/contest-accounts.mjs list
```
看該帳號是不是 `✗ 只能 OAuth`。是的話當場補一組密碼：
```bash
node scripts/contest-accounts.mjs set-password --names 該帳號
```

**選手機連不到判題機**

在選手機開命令提示字元 `ping 192.168.1.50`。
不通就是網路層問題（網路線、交換器、防火牆）；
通了但瀏覽器開不起來，多半是判題機沒加 `-H 0.0.0.0` 或防火牆沒放行 3000。

**提交一直停在 PENDING**

判題佇列塞住或判題服務掛了。確認 sandbox-server（`127.0.0.1:8090`）和
Piston（`localhost:2000`）還活著。伺服器重啟時 `instrumentation.ts` 會自動
把 PENDING / JUDGING 的提交重新排入佇列，不會遺失。

**交了但計分板沒動**

確認提交時有帶 `contestId`。從比賽頁面進去的題目才會帶，直接從 `/problems`
交的算一般練習，不計入計分板。
