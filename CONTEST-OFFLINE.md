# 斷網比賽操作手冊

機房沒有對外網路時的完整流程。給主辦 / 監考看的。

選手看的是 [使用說明書](client/使用說明書.html)（用瀏覽器開，可直接列印）；
收件程式的技術細節見 [client/README.md](client/README.md)。

---

## 這套東西怎麼運作

**伺服器不進機房。** itouOJ 照常跑在 `oj.itousouta.me` 上，判題也在那裡。
機房裡只有選手機和一支收件程式。

```
賽前（有網路）   每台選手機：登入 → 選比賽 → 對時 → 回報就緒
                  題目 PDF 放到桌面

比賽中（斷網）   收件程式把程式碼存在本機，一筆一個檔
                  完全不需要網路，也不需要區域網路

賽後（恢復網路） 每台自己按「回傳到伺服器」，判題結果照常出現在
                  網站上各自的帳號與計分板
```
---

## 一、賽前一週

### 1. 建立比賽

在 `/admin/contests` 建比賽、加題目、設定起訖時間。兩個必要設定：

- **計分方式選 IOI。** 賽後才整批判題，選手當下看不到結果，ICPC 的錯誤罰時
  會變成盲罰。IOI 每題只看最後一次提交、不罰時。
- **可用語言只勾 C++。** 勾了之後網頁提交、收件程式的測試執行與提交三條路
  都只收 C++，選手不會寫到一半才發現語言不對。

### 2. 確認選手都已報名

**這是最容易在當天爆掉的一項。** 沒報名的帳號，收件程式的比賽清單會標
「※尚未報名」，賽後上傳會被擋 403 —— 而且要到上傳那一刻才會發現，
整場的作答等於白做。

```bash
node scripts/contest-accounts.mjs list
```

需要批次開帳號時：

```bash
node scripts/contest-accounts.mjs create --prefix oj --count 30
node scripts/contest-accounts.mjs create --names alice,bob,carol
```

密碼隨機產生（去掉 `i l o 0 1` 這類容易看錯的字元），同時輸出到畫面和
`contest-accounts-<時間>.csv`，可直接列印裁成紙條。

> 資料庫只存 bcrypt hash，**關掉視窗後就再也查不回明碼**。CSV 請保管好，
> 賽後刪除。

> 收件程式改用瀏覽器登入之後，**只有 Google / Discord 的帳號也能正常使用**，
> 不必再事先補設密碼。舊版手冊要求的 `set-password --all-oauth-only`
> 已經不需要了。

### 3. 匯出題目 PDF

```bash
node scripts/export-problems.mjs --contest 2 --out C:\Contest\題目
```

輸出的是 HTML，用瀏覽器 `Ctrl+P` 另存成 PDF。渲染管線和網站一致
（remark-gfm + remark-math + rehype-katex），印出來和選手平常在 OJ 上看到的
長得一樣。

**檔名用題目代號**（`A.pdf`、`B.pdf`…），收件程式就能自動對應，選手在
「作答」分頁按「開啟題目」直接跳出來。

---

## 二、賽前一天：佈署選手機

### 1. 準備佈署包

從 [Releases](https://github.com/itousouta15/itouOJ/releases) 下載
**`itouOJ-lab-deploy-kit.zip`**，解壓到隨身碟或網路磁碟機。

> **不要讓每台機器各自從瀏覽器下載。** 下載的檔案會被系統加上
> Mark-of-the-Web，SmartScreen 會攔截未簽章的程式（這不是防毒判定為病毒，
> 是「未簽章 + 尚無信譽」造成的）。佈署包裡的安裝腳本會處理掉這個標記。

### 2. 每台機器跑一次安裝

執行佈署包裡的 **`install-on-this-pc.bat`**。腳本會：

- 複製到 `C:\itouOJ\`（沒有寫入權限時自動改裝到使用者目錄）
- 移除 Mark-of-the-Web
- 建立桌面捷徑
- 註冊 `itouoj://` 通訊協定（網站上「開啟收件程式」按鈕要靠它）

腳本只移除標記，不改動程式本體 —— 複製前後 SHA256 相同。

### 3. 每台機器完成賽前設定（**這一步需要網路**）

1. 開啟收件程式 → 按「用瀏覽器登入」→ 在瀏覽器用選手自己的帳號登入
2. 從清單選擇比賽
3. 「題目路徑與編譯器設定」→ 指定題目 PDF 資料夾

做完這三步，程式會把授權、題目對應表和**時鐘校正值**存進 `config.json`，
並自動向伺服器回報「這台機器已就緒」（含電腦名稱）。**做完就可以斷網了。**

設定可以在一台機器上做好之後把 `config.json` 複製到其他機器，但**登入不能共用**
—— 每位選手要用自己的帳號登入。

### 4. 確認 g++ 裝好了

收件程式的「測試執行」用本機 g++ 編譯。沒裝的話選手仍然可以提交
（提交不需要編譯），但就沒辦法在交之前自己驗一次。賽前在一台機器上
用佈署包裡的 `sample/a-plus-b.cpp` 試一次。

### 5. 上 PIN 鎖（選用）

「題目路徑與編譯器設定」裡可以設 PIN 並鎖定。鎖定後選手不能改比賽選擇與
管理員設定。**登入 / 登出區塊不受影響**，授權過期時仍可重新登入。

解鎖只在該次執行有效，關掉程式重開又是鎖著的。

> 這是**防呆，不是防駭**：擋的是比賽中手滑把比賽切掉。PIN 的雜湊存在本機，
> 有檔案存取權的人刪掉就解開了。真正的權限邊界在伺服器（報名檢查、
> 語言限制、時間夾制），客戶端動不了。

---

## 三、比賽當天

### 1. 開賽前的統一檢查（**最重要的一步**）

打開 **`/admin/contests/<比賽id>/status`**，確認：

```
已報名  30      收件程式已就緒  30 / 30      比賽狀態  尚未開始
```

只要不是 `30 / 30` 就代表有機器沒設定好。頁面會列出每個人的回報時間和
電腦名稱，可以直接對照是機房哪一台。**確認全部就緒再宣布開始。**

漏掉一台的代價是那位選手整場都交不出東西，而且要到賽後上傳時才會發現。

### 2. 統一開始

**不需要任何開始信號。** 每台機器在賽前設定時已經對過伺服器時鐘，
所以就算全場斷網，所有機器仍會在同一瞬間從「比賽尚未開始」的倒數畫面
自動切進作答畫面。

監考要做的只有一件事：**確認選手在開賽前不要關掉程式**。

要目視確認各機時鐘真的一致，看**視窗右下角的時間** —— 那是校正後的值，
全場每一台都應該顯示同一個時間。顯示成 `未校正` 的代表這台從來沒成功連過
伺服器，它的倒數會跟別人不同步，開賽前一定要處理掉。

### 3. 比賽中

程式會自己處理時間邊界 —— 時間到自動關閉提交入口，轉到上傳畫面。
監考不需要喊停也不需要按任何東西。

### 4. 賽後回收

恢復網路後，請選手各自按「回傳到伺服器」。

- 重複上傳不會產生重複提交（每筆有唯一鍵）
- 提交時間以校正過的時鐘為準，並在伺服器端夾制在比賽區間內
- 上傳後雙擊任一列可以跳到網站看判題結果

**在選手離開前確認每台都上傳成功。** 人走了機器關了，那些提交就只存在
本機硬碟上。

---

## 四、已知風險

### 判題佇列會在賽後被一次灌爆（最需要注意）

`src/lib/judge.ts` 的 `enqueueSubmission` 用一條 promise chain 串起所有提交，
**全站同一時間只有一筆在判**。

這個形式讓問題比線上賽更明顯：線上賽的提交是分散在兩小時裡的，
斷網賽是**全部集中在收網後那幾分鐘**一次湧入。30 人 × 每人 10 筆 × 每筆 10 測資，
最後一位可能要等上十幾分鐘才看得到結果。

緩解方式：

- **當天**：控制每題測資數量、壓低時限，讓單筆判題盡量在 2~3 秒內完成。
  請選手分批上傳而不是全部同時按。
- **中期**：改成 N 條平行 chain（N = CPU 核心數 − 1）。要注意平行執行會互相
  干擾 CPU time 量測，改完必須重新校正時限。

判題進度可以在 `/submissions` 看，PENDING 的數量會慢慢往下掉。

### 選手機時鐘不準

程式在賽前設定時會從伺服器 HTTP 回應的 `Date` 標頭算出校正值存起來，
之後全程用校正過的時間。所以機房時鐘不準沒關係，**但賽前設定那一步必須
真的連到伺服器**才會拿到校正值。

### 一台機器多人輪流用

正常關閉程式時會自動回到初始狀態（清掉帳號、比賽選擇、草稿），所以多半
不必特別交代。要立刻換人也可以手動按「賽前設定」→「登出」。

草稿一定要清：它按「比賽＋題號」存、不綁使用者，不清的話下一位選手打開
作答分頁會直接看到前一位寫的程式碼。

**但比賽進行中關閉程式不會清**，因為機房這時是斷網的、重新登入需要網路，
清掉等於那台機器再也回不來。還有未上傳的提交時同理也不清。

尚未上傳的提交任何情況下都不會被刪，只是標記成原本的帳號收著，
原本那位選手登回來還能補傳。

### 臨時改比賽時間或題目

改完之後選手機上的快取還是舊的。請選手按「賽前設定」→「更新比賽資訊」，
不要叫他們登出重登（會連草稿一起清掉）。

**但這需要網路。** 比賽已經斷網之後才改時間，是傳不到選手機上的 ——
所以要延長比賽就得在收網之後、或想辦法讓每台機器短暫連上一次。

---

## 五、疑難排解

**收件程式的比賽清單顯示「※尚未報名」**

該帳號沒有加入這場比賽。**賽前發現就還有救**，讓選手到 `/contests` 報名，
然後在收件程式按「重新整理比賽清單」。賽後才發現的話，補報名之後仍然可以
上傳（伺服器只檢查上傳當下有沒有報名）。

**按「用瀏覽器登入」沒有反應 / 瀏覽器沒有跳回來**

程式會在本機開一個只聽 `127.0.0.1` 的回呼埠。被防毒或防火牆擋掉時會卡住。
確認這台機器當下**有對外網路**（登入這一步需要），並允許程式接受本機連線。

**「開啟收件程式」按鈕在網站上沒作用**

`itouoj://` 通訊協定沒註冊成功。跑一次 `install-on-this-pc.bat`，
或直接從桌面捷徑開程式（功能完全一樣，只是要自己填伺服器網址）。

**測試執行說找不到編譯器**

這台沒裝 g++，或裝在非標準位置。在「題目路徑與編譯器設定」手動指定路徑。
不影響提交。

**上傳時說「有 N 筆提交不屬於目前登入的帳號，已跳過」**

這台機器上一位選手還有東西沒傳。請他登回來傳完，或確認他已經在別台傳過了。

**上傳後計分板沒動**

確認計分方式是 IOI，以及該題確實在這場比賽的題目清單裡。判題還沒跑完的話
計分板本來就不會有分數，先去 `/submissions` 看還有多少 PENDING。

**提交一直停在 PENDING**

判題佇列塞住或判題服務掛了。確認 sandbox-server（`127.0.0.1:8090`）還活著。
伺服器重啟時 `instrumentation.ts` 會自動把 PENDING / JUDGING 的提交重新排入
佇列，不會遺失。
